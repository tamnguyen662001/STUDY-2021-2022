CREATE PROC TINHTONG
( @A INT , @B INT, @SUMM INT OUTPUT)
AS

BEGIN
	SELECT @SUMM = @A + @B
END

DECLARE @SUM INT
SELECT @SUM=0
EXEC TINHTONG 2,5,@SUM OUTPUT
SELECT @SUM

GO
CREATE FUNCTION TINHTONG1 (@A INT,@B INT)
RETURNS INT
AS
BEGIN
	DECLARE @C INT
	SELECT @c = @A+@B
	RETURN @C
END

